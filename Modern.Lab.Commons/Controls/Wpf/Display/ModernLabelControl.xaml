<UserControl x:Class="Modern.Lab.Controls.Wpf.Display.ModernLabelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="120" d:DesignHeight="24">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Modern.Lab.Commons;component/Themes/Tokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- 타이포그래피가 Kind(토큰 타입 램프)를 따르는 텍스트와, 그 뒤에 붙는
         선택적 필수 필드 표시(빨간 별표). -->
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <!-- 세로 타이틀 바: Kind=Title이면서 TitleBar=True일 때만 표시되는
             액센트색 막대. 높이는 텍스트를 따라간다. -->
        <Border x:Name="TitleAccentBar"
                Width="3"
                Margin="0,1,8,1"
                Background="{StaticResource Brush.Accent}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Kind, ElementName=RootControl}" Value="Title" />
                                <Condition Binding="{Binding TitleBar, ElementName=RootControl}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
        <TextBlock x:Name="InnerText"
                   Text="{Binding Text, ElementName=RootControl}"
                   VerticalAlignment="Center"
                   TextTrimming="CharacterEllipsis">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <!-- 기본값(Body): 본문 크기, Regular 굵기, 기본 텍스트 색 -->
                    <Setter Property="FontFamily" Value="{DynamicResource Font.Family}" />
                    <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}" />
                    <Setter Property="FontWeight" Value="Normal" />
                    <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Kind, ElementName=RootControl}" Value="Title">
                            <Setter Property="FontSize" Value="{StaticResource Font.Size.Title}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Kind, ElementName=RootControl}" Value="Label">
                            <Setter Property="FontSize" Value="{StaticResource Font.Size.Label}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextSecondary}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Kind, ElementName=RootControl}" Value="Helper">
                            <Setter Property="FontSize" Value="{StaticResource Font.Size.Helper}" />
                            <Setter Property="FontWeight" Value="Normal" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextSecondary}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <!-- 필수 필드 표시: 토큰 주석에 따라 진한 빨강(ErrorBorder) 사용 -->
        <TextBlock Text="*"
                   Margin="3,0,0,0"
                   VerticalAlignment="Center"
                   FontWeight="SemiBold"
                   FontSize="{StaticResource Font.Size.Label}"
                   Foreground="{StaticResource Brush.ErrorBorder}">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Required, ElementName=RootControl}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </StackPanel>
</UserControl>
