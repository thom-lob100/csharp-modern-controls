namespace Modern.Lab.Controls.Wpf.Selection
{
    /// <summary>체크콤보 항목의 표시 스타일.</summary>
    public enum CheckItemStyle
    {
        /// <summary>모던 체크박스(둥근 사각 + 체크 표시). 다중 "선택/포함" 의미에 적합 — 기본값.</summary>
        CheckBox,

        /// <summary>온/오프 토글 스위치. 설정성 항목의 켬/끔 의미에 적합.</summary>
        Switch
    }
}
