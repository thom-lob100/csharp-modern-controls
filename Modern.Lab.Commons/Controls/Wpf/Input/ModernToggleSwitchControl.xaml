<UserControl x:Class="Modern.Lab.Controls.Wpf.Input.ModernToggleSwitchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:common="clr-namespace:Modern.Lab.Controls.Wpf.Common"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="140" d:DesignHeight="24">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <common:SharedResourceDictionary Source="/Modern.Lab.Commons;component/Themes/Tokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- 온/오프 토글 스위치: 알약형 트랙 + 원형 썸.
         켬 = 액센트 트랙 + 오른쪽 썸, 끔 = 회색 트랙 + 왼쪽 썸.
         체크콤보의 Switch 항목과 동일한 비주얼의 단독 버전. -->
    <CheckBox x:Name="InnerSwitch"
              HorizontalAlignment="Left"
              VerticalAlignment="Center"
              IsChecked="{Binding IsChecked, ElementName=RootControl, Mode=TwoWay}"
              Content="{Binding Text, ElementName=RootControl}"
              FontFamily="{DynamicResource Font.Family}"
              FontSize="{StaticResource Font.Size.Body}"
              Foreground="{StaticResource Brush.TextPrimary}"
              Cursor="Hand">
        <CheckBox.Template>
            <ControlTemplate TargetType="CheckBox">
                <StackPanel Orientation="Horizontal" Background="Transparent">
                    <Border x:Name="Track"
                            Width="34" Height="18"
                            CornerRadius="9"
                            Background="{StaticResource Brush.Border}"
                            VerticalAlignment="Center">
                        <Ellipse x:Name="Thumb"
                                 Width="12" Height="12"
                                 Fill="{StaticResource Brush.OnAccent}"
                                 HorizontalAlignment="Left"
                                 Margin="3,0,3,0" />
                    </Border>
                    <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="Track" Property="Background" Value="{StaticResource Brush.BorderHover}" />
                    </Trigger>
                    <!-- 켬 상태가 hover보다 우선하도록 뒤에 선언 -->
                    <Trigger Property="IsChecked" Value="True">
                        <Setter TargetName="Track" Property="Background" Value="{StaticResource Brush.Accent}" />
                        <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter TargetName="Track" Property="Background" Value="{StaticResource Brush.DisabledBackground}" />
                        <Setter Property="Foreground" Value="{StaticResource Brush.DisabledText}" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </CheckBox.Template>
    </CheckBox>
</UserControl>
